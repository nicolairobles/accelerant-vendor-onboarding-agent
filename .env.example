# Optional live synthesis provider. The app still falls back to deterministic synthesis
# when OPENAI_SYNTHESIS_PROVIDER is not set to "openai" or no API key is configured.
OPENAI_API_KEY=
OPENAI_SYNTHESIS_PROVIDER=deterministic
OPENAI_SYNTHESIS_MODEL=gpt-4o-mini-2024-07-18
